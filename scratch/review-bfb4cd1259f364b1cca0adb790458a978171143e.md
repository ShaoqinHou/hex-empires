---
schema: review-report/v1
commit: bfb4cd1259f364b1cca0adb790458a978171143e
iteration: 1
reviewer: sonnet
timestamp: 2026-04-18T05:46:05Z
verdict: PASS
summary: { BLOCK: 0, WARN: 0, NOTE: 2 }
---

## Commit

`test(hud): Phase 3.5+3.6 — stack summary + Tab cycling wrap tests`

## Summary

Adds 3 new test cases (88 lines) to the existing `TooltipOverlay.stackedUnits.test.tsx` file. All tests use concrete assertions, correct `HUDManagerProvider` wrapping, and proper `act()`/`fireEvent` patterns. No engine-rule violations present. Two informational notes raised.

## Findings

### F-a1b2c3d4
- **severity:** NOTE
- **file:** `packages/web/src/ui/hud/__tests__/TooltipOverlay.stackedUnits.test.tsx`
- **line:** 49
- **rule:** `testing.md` § "Concrete assertions / fixture setup"
- **offender:** `terrain: ALL_BASE_TERRAINS[0].id,`
- **message:** `makeTile` uses `ALL_BASE_TERRAINS[0].id` (array index `[0]`) to pick the terrain ID — order-dependent, silently breaks if barrel reorders entries; named constant `GRASSLAND.id` would be explicit and stable.
- **suggested-fix:** Replace `ALL_BASE_TERRAINS[0].id` with `GRASSLAND.id` imported from `@hex/engine`.
- **state:** open

### F-b3c4d5e6
- **severity:** NOTE
- **file:** `packages/web/src/ui/hud/__tests__/TooltipOverlay.stackedUnits.test.tsx`
- **line:** 247
- **rule:** `testing.md` § "Concrete assertions"
- **offender:** Test name — `'does NOT render stack summary when only units are on the tile (no city/improvement to summarize)'`
- **message:** Description says "no city/improvement" but actual guard is `stackSummaryParts.length > 1` — subtly wrong (two own units → `"1 unit"` = 1 part); misleads future contributors.
- **suggested-fix:** Reword to `"does NOT render stack summary when stackSummaryParts has only one entry (2 own units → '1 unit' = 1 part)"`.
- **state:** open

---

**runtime-model: claude-sonnet-4-6**
