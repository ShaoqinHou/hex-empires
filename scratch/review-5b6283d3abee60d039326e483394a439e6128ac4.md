---
schema: review-report/v1
commit: 5b6283d3abee60d039326e483394a439e6128ac4
iteration: 1
reviewer: sonnet
timestamp: 2026-04-18T05:50:00Z
verdict: PASS
summary: { BLOCK: 0, WARN: 2, NOTE: 1 }
---

## Commit

`feat(hud): Phase 3.4 — cycle marker ">" prefix on active entity in detailed tier`

## Summary

Phase 3.4 adds a `>` cycle marker span inside `DetailedTooltipBody` to visually indicate the active entity during Tab cycling. Implementation is correctly scoped to a UI HUD component, uses `var(--hud-tooltip-heading)` for color (no raw hex), and respects the `TooltipShell` wrapper. Two style-level violations exist (magic-number pixel offsets) and a test gap is noted, but there are no BLOCKs.

## Findings

### F-e3a6b780
- **severity:** WARN
- **file:** `packages/web/src/ui/hud/TooltipOverlay.tsx`
- **line:** 681–682
- **rule:** `ui-overlays.md` § "Positioning Rules"
- **offender:** `style={{ position: 'absolute', left: '-10px', ... top: '2px' }}`
- **message:** Cycle-marker span uses `position: 'absolute'` with hard-coded pixel values (`-10px`, `2px`) not from the spacing-token scale. `ui-overlays.md` forbids magic-number positioning as a drift vector.
- **suggested-fix:** Extract to a CSS custom property (e.g. `--hud-cycle-marker-gutter: -10px` in `hud-tokens.css`) or align via flexbox to eliminate absolute positioning entirely.
- **state:** open

### F-838a3fe9
- **severity:** WARN
- **file:** `packages/web/src/ui/hud/TooltipOverlay.tsx`
- **line:** 679
- **rule:** `testing.md` § "Layer 3: Behavioral (Browser)"
- **offender:** `data-testid="tooltip-cycle-marker"` — commit message explicitly defers test assertions
- **message:** New behavior (marker present on cycled entity, absent on enemies, absent when stack ≤ 1) has no test coverage in this commit. The `data-testid` was added "for future test assertions".
- **suggested-fix:** Add test to `TooltipOverlay.stackedUnits.test.tsx`: render warrior+settler stack with `isAltPressed={true}`, fire Tab once, assert `[data-testid="tooltip-cycle-marker"]` is present on the correct unit row.
- **state:** open

### F-8e85f817
- **severity:** NOTE
- **file:** `packages/web/src/ui/hud/TooltipOverlay.tsx`
- **line:** 685
- **rule:** `ui-overlays.md` § "Styling"
- **offender:** `fontSize: '10px'`
- **message:** Non-token magic number for font size; no `--hud-text-*` token at 10px exists. The existing cycle pill (line ~709) uses `11px`. Minor inconsistency.
- **suggested-fix:** Add `--hud-text-marker: 10px` to `hud-tokens.css` and use `fontSize: 'var(--hud-text-marker)'`, or use `11px` for visual consistency with the cycle pill.
- **state:** open

---

**runtime-model: claude-sonnet-4-6**
