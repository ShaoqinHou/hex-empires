---
schema: review-report/v1
commit: 4a2597b68e03ef511aaf8a1862603e6286b68a98
iteration: 1
reviewer: sonnet
timestamp: 2026-04-18T00:00:00Z
verdict: FAIL
summary: { BLOCK: 2, WARN: 1, NOTE: 0 }
---

## Summary

This commit adds the asset registry and loader for `packages/web/src/assets/` — a clean new module with correct `ReadonlyMap` types, named exports only, no `any`, and proper fallback semantics. One functional bug: two leader IDs in the registry use hyphens (`qin-shi-huang`, `genghis-khan`) while the engine canonical IDs use underscores (`qin_shi_huang`, `genghis_khan`), meaning `getLeaderPortrait` will always silently fall back to the placeholder for those leaders. One advisory: `process.env.NODE_ENV` is used for the dev-warning guard where Vite's `import.meta.env.DEV` would be cleaner and require no type cast.

## Findings

### F-40c70d9c
- severity: block
- file: packages/web/src/assets/registry.ts
- line: 45
- rule: data-driven.md § "Referenced IDs must exist in their respective registries"
- offender: `['qin-shi-huang', { path: '/assets/images/leaders/qin-shi-huang.webp', ... }]`
- message: Registry key `'qin-shi-huang'` uses hyphens but the engine canonical `LeaderId` is `'qin_shi_huang'` (underscore per `all-leaders.ts` and `GameInitializer.ts`), so `getLeaderPortrait('qin_shi_huang')` always falls back to the silhouette placeholder at runtime.
- suggested-fix: Change the map key to `'qin_shi_huang'` (file path string can stay as-is or be updated independently).
- state: open

### F-e9aa4184
- severity: block
- file: packages/web/src/assets/registry.ts
- line: 48
- rule: data-driven.md § "Referenced IDs must exist in their respective registries"
- offender: `['genghis-khan', { path: '/assets/images/leaders/genghis-khan.webp', ... }]`
- message: Registry key `'genghis-khan'` uses hyphens but the engine canonical `LeaderId` is `'genghis_khan'` (underscore), so portrait lookup for this leader always silently returns the fallback silhouette.
- suggested-fix: Change the map key to `'genghis_khan'`.
- state: open

### F-48873017
- severity: warn
- file: packages/web/src/assets/loader.ts
- line: 43
- rule: tech-conventions.md § "TypeScript strict mode / ESM only"
- offender: `(process as { env?: { NODE_ENV?: string } }).env?.NODE_ENV !== 'production'`
- message: Dev-warning guard uses `process.env.NODE_ENV` with a manual type cast; in a Vite project the idiomatic guard is `import.meta.env.DEV` which is typed by Vite's `ImportMeta` declaration, requires no cast, and tree-shakes reliably.
- suggested-fix: Replace the entire condition with `import.meta.env.DEV`.
- state: open

## Cross-file findings

None. The three files (`registry.ts`, `loader.ts`, `index.ts`) form a self-contained module with no cross-package boundary violations.
