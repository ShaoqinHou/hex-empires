---
schema: review-report/v1
commit: 0f62d15943a3eb8e472a034451a01ce00f7b5715
iteration: 1
reviewer: sonnet
timestamp: 2026-04-20T00:36:16Z
verdict: PASS
summary: { BLOCK: 0, WARN: 0, NOTE: 1 }
---

## Summary

Both claimed fixes are correctly implemented and complete. F-dbb59dc5 (cross-system import of `enqueueDiscoveryEvent` from `narrativeEventSystem`) is fully resolved: `movementSystem` now imports from `../state/narrativeEventUtils`, which lives in `state/` with only `types/` imports — fully within import-boundary rules. F-5d0e6027 (missing ageGate check in `evaluateAndEnqueue`) is fully resolved: `isAgeGateOpen` is called at `narrativeEventSystem.ts:46` inside the filter predicate. One NOTE is raised for a missing test case covering the new ageGate path in `enqueueDiscoveryEvent`.

## Findings

### F-e2a48a2c
- severity: note
- file: packages/engine/src/systems/__tests__/narrativeEventSystem.test.ts
- line: 244
- rule: testing.md § "Layer 1: Unit Tests (System Logic)"
- offender: describe('enqueueDiscoveryEvent (F-06)')
- message: The `enqueueDiscoveryEvent` test suite covers dedup, unknown-event no-op, and basic enqueue, but has no case exercising the ageGate check — the core fix this commit introduced for the discovery path.
- suggested-fix: Add a test with `ageGate: 'exploration'` on the DISCOVERY_EVENT, state at `currentAge: 'antiquity'`, and assert `enqueueDiscoveryEvent` returns the same state reference (event blocked by age gate).
- state: open

## Cross-file findings

None.

## Fix verification

**F-dbb59dc5 (cross-system import — CLOSED):**
- Old: movementSystem.ts:5 imported from './narrativeEventSystem' — system importing from sibling system (BLOCK per import-boundaries.md).
- New: imports from '../state/narrativeEventUtils' — allowed direction (systems/ -> CAN import from: state/).
- narrativeEventUtils.ts imports only from '../types/GameState' and '../types/NarrativeEvent' — clean state/ -> types/ boundary.
- narrativeEventSystem.ts:172 re-exports both symbols for barrel compatibility (index.ts:334). Re-export is structurally valid.

**F-5d0e6027 (missing ageGate in evaluateAndEnqueue — CLOSED):**
- Old: evaluateAndEnqueue never called isAgeGateOpen — events with a future-age gate could fire in the wrong age.
- New: narrativeEventSystem.ts:46 adds `if (!isAgeGateOpen(e, state)) return false;` inside the .filter() predicate before matchesRequirements.
- enqueueDiscoveryEvent in narrativeEventUtils.ts:25 also gates on isAgeGateOpen — consistent across both trigger paths.

**Semantic equivalence check — enqueueDiscoveryEvent refactor:**
The deleted implementation called the private matchesRequirements(def.requirements, player, state). The new narrativeEventUtils.ts inlines identical logic (lines 30-45): requiresTags, excludesTags, leaderId, civId — all four checks present in the same order. No behavioral regression introduced.
