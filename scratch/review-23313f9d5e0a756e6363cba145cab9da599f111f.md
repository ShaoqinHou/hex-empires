---
schema: review-report/v1
commit: 23313f9d5e0a756e6363cba145cab9da599f111f
iteration: 1
reviewer: sonnet
timestamp: 2026-04-20T00:00:00Z
verdict: PASS
summary: { BLOCK: 0, WARN: 1, NOTE: 0 }
---

## Summary

Mechanical Cycle F cleanup: removes `buildingPlacementSystem` from `DEFAULT_SYSTEMS` (superseded by `adaptUrbanBuilding`/`urbanBuildingSystem` wired in M12), marks the legacy file `@deprecated`, and swaps `calculateCityYields` for `calculateCityYieldsWithAdjacency` in `growthSystem` and `productionSystem`. All three changes are well-scoped and import-boundary-clean. One warn: `PLACE_BUILDING` actions are now silently swallowed by the engine with no deprecation guard.

## Findings

### F-9b8b494e
- severity: warn
- file: packages/engine/src/systems/buildingPlacementSystem.ts
- line: 81
- rule: engine-patterns.md -- "Systems process only actions they care about, return state unchanged for others" / architecture.md -- "System Pipeline Pattern"
- offender: `export function buildingPlacementSystem(state: GameState, action: GameAction): GameState`
- message: With `buildingPlacementSystem` removed from `DEFAULT_SYSTEMS`, any `PLACE_BUILDING` action dispatched through `GameEngine.applyAction` is now a silent no-op -- the action matches no wired system and the state is returned unchanged with no log entry or `lastValidation` error.
- suggested-fix: Add a dead-action guard in `GameEngine.applyAction` (or in `urbanBuildingSystem`) that sets `lastValidation: { valid: false, reason: 'PLACE_BUILDING is deprecated; use PLACE_URBAN_BUILDING', category: 'general' }` when `action.type === 'PLACE_BUILDING'` reaches the pipeline, so callers get an explicit failure instead of a silent no-op.
- state: open

## Cross-file findings

None.
