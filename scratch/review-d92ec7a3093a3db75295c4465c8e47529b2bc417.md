---
schema: review-report/v1
commit: d92ec7a3093a3db75295c4465c8e47529b2bc417
iteration: 1
reviewer: sonnet
timestamp: 2026-04-18T06:00:00Z
verdict: PASS
summary: { BLOCK: 0, WARN: 2, NOTE: 4 }
---

## Summary

All five BLOCKs cited in the commit message are correctly and completely resolved. The
`as unknown as { achievements? }` type hole is gone; `unlockedAchievements` is properly
typed on `GameState`, initialized in `GameInitializer`, and read/written directly in
both the system and the panel. The three raw-hex chrome violations in `AchievementsPanel`
and `TopBar` are tokenized. Two pre-existing raw-rgba lines in `TopBar.tsx` remain (not
introduced by this diff, but in a file the diff touched) and are flagged as WARNs. One
cross-file type-precision NOTE on `ReadonlyArray<string>` vs `ReadonlyArray<AchievementId>`.

## Findings

### F-789a3890
- severity: note
- file: packages/web/src/ui/layout/TopBar.tsx
- line: 83
- rule: panels.md § "Styling"
- offender: `rgba(186, 104, 200, 0.9)` (prior)
- message: RESOLVED — influence chip now uses `var(--color-influence)`; token defined in `index.css`.
- state: closed

### F-c69c9467
- severity: note
- file: packages/web/src/ui/layout/TopBar.tsx
- line: 150
- rule: panels.md § "Styling"
- offender: `rgba(34, 197, 94, 0.4)` (prior)
- message: RESOLVED — end-turn non-ready branch shadow now uses `color-mix(in srgb, var(--color-food) 40%, transparent)`.
- state: closed

### F-b3bffbb9
- severity: note
- file: packages/web/src/ui/panels/AchievementsPanel.tsx
- line: 62
- rule: panels.md § "Styling"
- offender: `background: '#30363d'` (prior)
- message: RESOLVED — divider background is now `var(--panel-border)`.
- state: closed

### F-de52880b
- severity: note
- file: packages/web/src/ui/panels/AchievementsPanel.tsx
- line: 113
- rule: panels.md § "Styling"
- offender: `rgba(88, 166, 255, 0.08)` / `rgba(139, 148, 158, 0.04)` (prior)
- message: RESOLVED — `--panel-card-earned-bg` and `--panel-card-locked-bg` tokens added to `panel-tokens.css`; component references `var()`.
- state: closed

### F-bc7658f6
- severity: note
- file: packages/engine/src/types/GameState.ts
- line: 380
- rule: tech-conventions.md § "Strict mode, no any"
- offender: `state as unknown as { achievements?: ... }` (prior)
- message: RESOLVED — `readonly unlockedAchievements?: ReadonlyMap<PlayerId, ReadonlyArray<string>>` added to `GameState`, initialized in `GameInitializer`, system and panel read/write it directly without casts.
- state: closed

---

### F-f783372d
- severity: warn
- file: packages/web/src/ui/layout/TopBar.tsx
- line: 67
- rule: panels.md § "Styling"
- offender: `boxShadow: '0 2px 4px rgba(88, 166, 255, 0.3)'`
- message: Pre-existing raw-rgba on the turn-badge shadow; TopBar was modified in this diff but this line was not tokenized, leaving the file in a mixed state after the chrome-raw-hex regression fix.
- suggested-fix: Use `color-mix(in srgb, var(--color-science) 30%, transparent)` or add `--panel-turn-badge-shadow` to `panel-tokens.css`.
- state: open

### F-57e94748
- severity: warn
- file: packages/web/src/ui/layout/TopBar.tsx
- line: 89
- rule: panels.md § "Styling"
- offender: `backgroundColor: 'rgba(77, 171, 247, 0.1)'`
- message: Pre-existing raw-rgba on the research-progress chip background; not introduced by this commit but in the same file that was partially tokenized — inconsistent cleanup.
- suggested-fix: Use `color-mix(in srgb, var(--color-science) 10%, transparent)` or add a `--panel-research-chip-bg` token.
- state: open

### F-26af39fe
- severity: note
- file: packages/engine/src/systems/achievementSystem.ts
- line: 25
- rule: tech-conventions.md § "Strict mode, no any"
- offender: `(state.unlockedAchievements?.get(playerId) ?? []) as ReadonlyArray<AchievementId>`
- message: The `as` cast widens `ReadonlyArray<string>` (GameState field) to `ReadonlyArray<AchievementId>` (branded type). Safe today but a future writer passing `string[]` would silently produce an unsound value with no compile-time error.
- suggested-fix: Type `GameState.unlockedAchievements` as `ReadonlyMap<PlayerId, ReadonlyArray<AchievementId>>` and remove the cast.
- state: open

### F-31d88527
- severity: note
- file: packages/engine/src/types/GameState.ts
- line: 380
- rule: tech-conventions.md § "Strict mode, no any"
- offender: `readonly unlockedAchievements?: ReadonlyMap<PlayerId, ReadonlyArray<string>>;`
- message: Using `string` rather than `AchievementId` for the array element type is the root cause of the cast in `achievementSystem.ts`. Importing `AchievementId` into `types/GameState.ts` is permitted by import-boundaries.
- suggested-fix: Change element type to `AchievementId`.
- state: open

## Cross-file findings

F-26af39fe and F-31d88527 form a single type-precision gap: `GameState.ts` declares `ReadonlyArray<string>` and `achievementSystem.ts` casts it back to `ReadonlyArray<AchievementId>` on read. Fixing the `GameState` field type eliminates the cast.
