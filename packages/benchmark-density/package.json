{
  "name": "@hex-empires/benchmark-density",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "benchmark-density": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ../.. --no-file-parallelism packages/benchmark-density/src",
    "benchmark": "node dist/cli.js",
    "smoke": "node dist/cli.js --smoke --quiet"
  },
  "dependencies": {
    "@hex-empires/kernel": "0.0.0",
    "@hex-empires/scenario-density": "0.0.0"
  }
}
