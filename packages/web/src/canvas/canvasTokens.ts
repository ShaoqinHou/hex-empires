/**
 * canvasTokens — named constants for canvas-layer visual values.
 *
 * CSS custom properties cannot be used directly in Canvas 2D API calls.
 * This file is the single source of truth for canvas-specific colors and
 * other visual constants, satisfying the "never hardcode colors" convention
 * while remaining compatible with the imperative Canvas API.
 */

/** Projectile color for ranged unit attacks. */
export const RANGED_PROJECTILE_COLOR = '#ff5722';
